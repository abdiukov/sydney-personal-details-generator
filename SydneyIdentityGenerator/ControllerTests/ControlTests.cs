namespace ControllerTests;
class ControlTests
{
    // Tests to add
    // 1. GeneratePersonsAndWriteToCsv -> should call all methods
    // 2. GeneratePersonsAndWriteToCsv -> if error occurred, should return task failure
    // 3. GeneratePersons => Should generate expected amount of persons
    // 4. GeneratePersons => Should work if delegate is null
    // 5. GeneratePersons => Should work if delegate contains everything
    // 6. GeneratePersons => Should contain roughly the same amount of males and females
}